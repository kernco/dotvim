Installation:

    git clone git://github.com/kernco/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc

Install submodules:

    cd ~/.vim
    git submodule init
    git submodule update


